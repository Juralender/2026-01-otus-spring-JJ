fullnameOverride: library-postgresql

image:
  repository: postgres
  tag: "18-trixie"
  pullPolicy: IfNotPresent

auth:
  database: library
  existingSecret: db-credentials
  secretKeys:
    usernameKey: username
    passwordKey: password

service:
  port: 5432

persistence:
  size: 1Gi
  storageClassName: ""
